<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mybookreader">
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@drawable/ic_launcher_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyBookReader">
        <activity
            android:name=".activities.MainScreenActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:name=".activities.AddBookToBookShelfActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AddBookShelfActivity"
            android:exported="false" />
        <activity
            android:name=".activities.OpenedBookshelfActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AddBookActivity"
            android:exported="false" />
        <activity
            android:name=".activities.PDFOpenerActivity"
            android:exported="false" />
        <activity
            android:name=".activities.FadingSplashScreenActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>